if测试和语法规则

当语句太长、难以单放在一行时，有些特殊的规则可用于使其位于多行之中
如果使用语法括号对，语句就可横跨数行
如果语句以反斜杠结尾，就可横跨数行
字符串的三重引号

真值测试

and or运算符总会返回对象
and返回第一个为假的对象，如果都为真则返回最右边的对象
or返回第一个为真的对象，如果都为假则返回最右边的对象

if/else三元表达式

A=Y if X else Z
